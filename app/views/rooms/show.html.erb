<h1><%= @room.name %></h1>

Tools in the room <br>
    <% @room.tools.each do |tool| %>
        <p>- <%= tool.name %></p>
    <% end %>

<h1>Inventory</h1>
<p>You have a <%= Room.all.last.tools.last.name %>.</p>
<h1>Time</h1>
<p><%= (Time.now - @playTime).to_i %> seconds.</p>

<p><%= link_to "Back to the House", "/rooms" %></p>